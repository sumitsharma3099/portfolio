<!DOCTYPE html>
<html>

<head>
  <!-- Page title -->
  <title>Portfolio</title>

  <!-- Viewport settings for responsive design -->
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- CSRF (Cross-Site Request Forgery) meta tags -->
  <%= csrf_meta_tags %>

  <!-- Content Security Policy meta tag -->
  <%= csp_meta_tag %>

  <!-- Stylesheets -->
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <!-- JavaScript Importmap tags -->
  <%= javascript_importmap_tags %>
</head>

<body class="flex flex-col min-h-screen font-sans bg-gray-100">
  <div class="flex-grow">
    <!-- Render navigation layout -->
    <%= render 'layouts/navigation' %>

    <!-- Render head layout (assuming it includes any additional styles or meta tags) -->
    <%= render 'layouts/head' %>

    <div class="container mx-auto p-4">
      <!-- Page content (yield) -->
      <%= yield %>
    </div>
  </div>

  <!-- Render footer layout -->
  <%= render 'layouts/footer' %>
</body>

</html>
